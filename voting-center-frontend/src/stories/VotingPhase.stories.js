import VotingPhase from '../components/VotingPhase'

export default {
    title: 'Components/VotingPhase',
    component: VotingPhase,
}

const Template = (args) => <VotingPhase {...args} />

export const Default = Template.bind({})
Default.args = {}
